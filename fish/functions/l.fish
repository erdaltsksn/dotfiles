# ---------------------- general ----------------------------------------------#

function l --description "List files in long format with grouping and classification"
    ls -l --almost-all --group --group-directories-first --classify
end
